{
  "name": "@monou/agent",
  "version": "0.1.0",
  "description": "启动 agent 的 app：连 Gateway、加载 .u、执行 runTurn",
  "type": "module",
  "main": "./dist/index.js",
  "bin": { "monou-agent": "./dist/index.js" },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npx tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "dependencies": {
    "@monou/agent-from-dir": "^0.1.0",
    "@monou/agent-sdk": "^0.1.0",
    "@monou/cron": "^0.1.0",
    "dotenv": "^16.6.1",
    "ws": "^8.18.0"
  },
  "engines": { "node": ">=20.0.0" },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/ws": "^8.5.13",
    "typescript": "^5.9.2"
  }
}
