{
  "name": "@monou/gateway",
  "version": "0.1.0",
  "description": "Gateway protocol types and client for monoU (callGateway, RPC)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "README.md"],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npx tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": ["monou", "gateway", "rpc", "protocol"],
  "author": "",
  "license": "MIT",
  "engines": { "node": ">=20.0.0" },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/ws": "^8.5.13",
    "typescript": "^5.9.2"
  }
}
