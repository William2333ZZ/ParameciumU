{
  "name": "@monou/agent-template",
  "version": "0.1.0",
  "description": "Agent directory template and path helpers for .u",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "template", "README.md"],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npx tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run clean && npm run build",
    "test": "vitest --run"
  },
  "keywords": ["monou", "agent", "template"],
  "author": "",
  "license": "MIT",
  "engines": { "node": ">=20.0.0" },
  "devDependencies": {
    "@monou/agent-core": "^0.1.0",
    "@types/node": "^22.10.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
