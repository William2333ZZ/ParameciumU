{
	"name": "@monou/agent-core",
	"version": "0.1.0",
	"description": "Agent runtime: state, loop, and transport abstraction",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"files": ["dist", "README.md"],
	"scripts": {
		"clean": "rm -rf dist",
		"build": "npx tsc -p tsconfig.build.json",
		"dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
		"test": "vitest --run --passWithNoTests",
		"prepublishOnly": "npm run clean && npm run build"
	},
	"dependencies": { "@monou/shared": "^0.1.0" },
	"keywords": ["monou", "agent", "runtime", "state"],
	"author": "",
	"license": "MIT",
	"engines": { "node": ">=20.0.0" },
	"devDependencies": {
		"@types/node": "^22.10.5",
		"typescript": "^5.9.2",
		"vitest": "^3.2.4"
	}
}
