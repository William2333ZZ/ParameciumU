# 复制为 .env 并填入，不要提交 .env
# cp env.example .env
# Agent 的 LLM 配置以「agent 目录下的 llm.json」为准（见 docs/start/getting-started.md）。
# 以下仅作 llm.json 缺项时的回退（可选）：
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
